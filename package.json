{
  "name": "mdglasses",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "WEBKIT_DISABLE_COMPOSITING_MODE=1 GDK_BACKEND=x11 tauri dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:rust": "cd src-tauri && cargo test",
    "dev:wait": "concurrently \"vite\" \"wait-on http://localhost:1420\""
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-opener": "^2.0.0",
    "github-markdown-css": "^5.6.0",
    "highlight.js": "^11.10.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "happy-dom": "^20.5.3",
    "typescript": "~5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "wait-on": "^8.0.0",
    "concurrently": "^9.0.0"
  }
}
